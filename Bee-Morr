import streamlit as st

# --- PAGE CONFIG ---
st.set_page_config(page_title="Beeai Morrison | Scriptwriter & Strategist", page_icon="✒️")

# --- BLUE & WHITE STYLE ---
st.markdown("""
    <style>
    .stApp {
        background-color: #FFFFFF;
    }
    h1 {
        color: #003366 !important;
        font-size: 3rem !important;
        font-weight: 800 !important;
    }
    h2, h3 {
        color: #00509E !important;
    }
    p {
        color: #333333;
        font-size: 1.2rem;
    }
    .stButton>button {
        background-color: #003366 !important;
        color: white !important;
        width: 100%;
        border-radius: 50px;
        height: 3.5rem;
        font-size: 1.2rem;
        font-weight: bold;
        border: none;
        box-shadow: 0px 4px 10px rgba(0,0,0,0.1);
    }
    .hero-box {
        padding: 40px;
        border-left: 10px solid #003366;
        background-color: #F0F7FF;
        margin-bottom: 30px;
    }
    </style>
    """, unsafe_allow_html=True)

# --- HERO SECTION ---
st.markdown('<div class="hero-box">', unsafe_allow_html=True)
st.title("Beeai Morrison")
st.subheader("Where Technical Logic Meets Creative Conversion.")
st.write("""
I am a Professional Scriptwriter and M.Sc. Candidate. 
I don't just write scripts—I design communication frameworks that drive results.
""")
st.markdown('</div>', unsafe_allow_html=True)

# --- THE THREE PILLARS ---
col1, col2, col3 = st.columns(3)

with col1:
    st.markdown("### ✍️ Scriptwriting")
    st.write("Direct-response video scripts for health, tech, and lifestyle brands.")

with col2:
    st.markdown("### 📈 SEO Strategy")
    st.write("Soulful, high-impact metadata for YouTube growth and visibility.")

with col3:
    st.markdown("### 🎓 Research")
    st.write("Specialized in system design and logic-based communication models.")

st.divider()

# --- CASE STUDY / PROOF ---
st.header("The Results")
tab1, tab2 = st.tabs(["Marketing Success", "Academic Depth"])

with tab1:
    st.write("**Orvella Joint Support:** Developed high-converting video ad scripts and SEO strategies.")
    st.write("**SoundsForSleep:** Managing SEO and metadata for a soulful, relaxation-focused channel.")

with tab2:
    st.write("**M.Sc. Dissertation:** Engineering a USSD-based emergency response logic for low-connectivity environments.")

st.divider()

# --- CALL TO ACTION (THE LANDING) ---
st.header("Ready to elevate your project?")
st.write("Click below to discuss your next script, strategy, or research project.")

c1, c2 = st.columns(2)
with c1:
    if st.button("Hire Me for Scriptwriting"):
        st.balloons()
        st.success("Redirecting to Email...")
with c2:
    if st.button("Book a Strategy Call"):
        st.info("Opening Calendar...")

st.sidebar.markdown("---")
st.sidebar.write("© 2026 Beeai Morrison")
